
# Main
app-url                                   = ${APP_URL:http://localhost:8080}
spring.application.name                   = ${APP_NAME:demo}
debug                                     = ${APP_DEBUG:false}
trace                                     = ${APP_TRACE:false}

# Port
server.port                               = ${APP_PORT:8080}

#database
spring.datasource.url                     = jdbc:mysql://127.0.0.1:3306/demo?serverTimezone=UTC&useSSL=false
spring.datasource.username                = root
spring.datasource.password                = mysql123
spring.jpa.properties.hibernate.dialect   = org.hibernate.dialect.MySQL5InnoDBDialect
spring.jpa.hibernate.ddl-auto             = update

spring.resources.static-locations         = classpath:/public/,file:storage

# File upload
spring.servlet.multipart.max-file-size    = 10MB
spring.servlet.multipart.max-request-size = 10MB